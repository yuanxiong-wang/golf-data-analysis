# Publishing

Before pushing changes to `docs/junior-golf-report/` or its published data,
update the visible `Generated` date in `docs/junior-golf-report/index.html` to
the generation date of the newest published snapshot. Verify the date and all
download links before pushing.
